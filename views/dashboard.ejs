<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Redwave Host - Dashboard</title>
    <link rel="stylesheet" href="/style.css">
    <script src="/socket.io/socket.io.js"></script>
</head>
<body>
    <div class="sidebar">
        <h2>Redwave⚡</h2>
        <a href="/" class="menu-item active">Meus Servidores</a>
        <a href="#" class="menu-item">Créditos</a>
        <a href="#" class="menu-item">Sair</a>
    </div>

    <div class="main-content">
        <div class="header">
            <h1>Seus Bots</h1>
        </div>

        <div class="card" style="margin-bottom: 30px;">
            <h3>Clonar Novo Servidor do GitHub</h3>
            <form action="/create" method="POST" style="display: flex; flex-wrap: wrap; gap: 10px;">
                <select name="template" required style="width: auto; flex-grow: 1; max-width: 250px;">
                    <option value="" disabled selected>Escolha o Bot para Clonar</option>
                    <% botTemplates.forEach(template => { %>
                        <option value="<%= template.name %>"><%= template.name %> (Clona de <%= template.url.split('/').pop().replace('.git', '') %>)</option>
                    <% }) %>
                </select>
                <input type="text" name="name" placeholder="Nome do Servidor (ex: MeuBotNovo)" required style="flex-grow: 2;">
                <button type="submit" class="btn btn-primary" style="flex-grow: 1;">Clonar e Criar</button>
            </form>
            <p style="font-size: 0.8em; color: var(--text-muted); margin-top: 10px;">
                ⚠️ **Atenção:** Após clonar, você precisa rodar `npm install` manualmente na pasta do bot antes de INICIAR.
            </p>
        </div>

        <div class="server-grid" id="server-grid">
            <% servers.forEach(server => { %>
                <div class="card" id="card-<%= server.id %>">
                    <div style="display:flex; justify-content:space-between;">
                        <h3><%= server.name %></h3>
                        <span class="status-dot <%= server.status %>" id="dot-<%= server.id %>"></span>
                    </div>
                    <p style="color: var(--text-muted);"><%= server.type %></p>
                    <p>Status: <strong id="status-<%= server.id %>"><%= server.status.toUpperCase() %></strong></p>
                    <a href="/server/<%= server.id %>">
                        <button class="btn btn-primary" style="width: 100%; margin-top: 10px;">Gerenciar</button>
                    </a>
                </div>
            <% }) %>
        </div>
    </div>
    
    <script>
        const socket = io();
        
        socket.on('status-change', (data) => {
            const dot = document.getElementById(`dot-${data.id}`);
            const statusText = document.getElementById(`status-${data.id}`);
            
            if (dot && statusText) {
                // Adiciona a classe 'cloning' se o status for esse
                dot.classList.remove('online', 'offline', 'iniciando', 'cloning', 'error');
                dot.classList.add(data.status);
                
                statusText.textContent = data.status.toUpperCase();
            }
        });
    </script>
</body>
</html>
